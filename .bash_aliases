alias g="git"
alias ano="git status -s"
alias very="git"
alias such="git"
alias wow="git status -s"
alias anoba="git log --pretty=oneline --abbrev-commit"
# TODO: create command to automatically pull the following down from some gist
#[alias]
    # one-line log
#    l = log --pretty=format:"%C(yellow)%h\\ %ad%Cred%d\\ %Creset%s%Cblue\\ [%cn]" --decorate --date=short

#    a = add
#    ap = add -p
#    c = commit --verbose
#    ca = commit -a --verbose
#    cm = commit -m
#    cam = commit -a -m
#    m = commit --amend --verbose
    
#    d = diff
#    ds = diff --stat
#    dc = diff --cached

#    s = status -s
alias git-co='git checkout'
alias git-cob='git checkout -b'
    # list branches sorted by last modified
#    b = "!git for-each-ref --sort='-authordate' --format='%(authordate)%09%(objectname:short)%09%(refname)' refs/heads | sed -e 's-refs/heads/--'"

    # list aliases
#    la = "!git config -l | grep alias | cut -c 7-"
